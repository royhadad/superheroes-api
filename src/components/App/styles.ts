import {makeStyles} from "@material-ui/core/styles";

const styles = makeStyles((theme) => {
    return {
        root: {
            width: '100%',
            maxWidth: '800px',
            margin: '20px auto'
        },
        title: {},
        search: {}
    }
})

export default styles;